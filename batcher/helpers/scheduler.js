/** @param {NS} ns */
/**
 * [Handles monitoring of batch completion times, and scheduling of new batches]
 * @param {Array} serverList [Array of server objects]
 */

export async function main(ns, serverList) {
    
}