/** @param {NS} ns */
/**
 * [Creates and deploys batches]
 * @param {Array} serverList [Array of server objects]
 */

import { crawl } from "batcher/helpers/crawler";

export async function main(ns) {
    let targetHackTime;
    let targetGrowTime;
    let targetWeakTime;
}